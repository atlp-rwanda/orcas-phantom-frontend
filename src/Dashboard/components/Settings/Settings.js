import React from 'react';
import Styled from '@emotion/styled';

const Header = Styled.h1`
	position: absolute;
	left: 290px;
	top: 130px; 
`;

const Settings = () => {
  return <Header>Settings component</Header>
}

export default Settings;